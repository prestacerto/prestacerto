import { getNoIndexMetadata } from '@/lib/seo/metadata';
export const metadata = getNoIndexMetadata('Painel de mercado');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
