import { getNoIndexMetadata } from '@/lib/seo/metadata';
export const metadata = getNoIndexMetadata('Administração');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
